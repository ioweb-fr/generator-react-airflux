import {Model} from "simod";

class <%= pascalName %> extends Model {

    constructor(attrs) {
        super({
            /** EDIT ME */
        }, attrs);
    }

}

export default <%= pascalName %>;
